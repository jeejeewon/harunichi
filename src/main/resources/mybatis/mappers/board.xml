<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mapper.board">

	<select id="selectBoardById" parameterType="int"
		resultType="com.harunichi.board.vo.BoardVo">
		SELECT boardId, boardCont, boardDate, boardImg, boardLike, boardCount,
		boardRe
		FROM board
		WHERE boardId = #{boardId}
	</select>

	<select id="selectBoardList"
		resultType="com.harunichi.board.vo.BoardVo">
		SELECT boardId, boardCont, boardDate, boardImg, boardLike, boardCount,
		boardRe
		FROM board
		ORDER BY boardDate DESC
	</select>
	
    <insert id="insertBoard" parameterType="com.harunichi.board.vo.BoardVo">
        INSERT INTO board (boardCont, boardDate, boardImg, boardLike, boardCount, boardRe)
        VALUES (#{boardCont}, #{boardDate}, #{boardImg}, #{boardLike}, #{boardCount}, #{boardRe})
    </insert>
	

</mapper>